/** String Template
 * Declare variables product and price of type string and number, respectively.
 * Create a string using template literals to display the product and its price 
 * in the format "The product {product} is priced at {price} dollars."
 */

let product: string = "Shampoo";
let price: number = 10;
console.log(`The product ${product} is priced at ${price} dollars`);
